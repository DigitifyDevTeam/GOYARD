# Production environment for server (goyard-demenagement.fr)
# On the server: copy this to load_env.sh and run: source load_env.sh
# Or: cp env.production.example load_env.sh then edit secrets (DJANGO_SECRET_KEY, GOOGLE_MAPS_API_KEY).

# Django
export DJANGO_SETTINGS_MODULE=core.settings.production
export PYTHONPATH="${PYTHONPATH}:$(pwd)"
export DJANGO_SECRET_KEY="CHANGE-THIS-TO-A-SECURE-SECRET-KEY"

# MySQL/MariaDB Database Configuration
export DB_NAME=qe2rfm_dev
export DB_USER=qe2rfm_dev
export DB_PASSWORD=Abra5687ca77dabra.
export DB_HOST=localhost
export DB_PORT=3306

# Allowed hosts (comma-separated, no spaces)
export ALLOWED_HOSTS=goyard-demenagement.fr,www.goyard-demenagement.fr

# CORS (comma-separated origins, no trailing slashes)
export CORS_ALLOW_ALL_ORIGINS=False
export CORS_ALLOWED_ORIGINS=https://goyard-demenagement.fr,https://www.goyard-demenagement.fr

# Optional: Google Maps API key for frontend/backend
export GOOGLE_MAPS_API_KEY=""

echo "Environment variables loaded for production"
