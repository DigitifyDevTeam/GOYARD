/** Shared sizing for gradient banner CTAs (devis + contact + phone pill). */
export const gradientCtaActionsClass =
  "flex flex-col sm:flex-row flex-wrap gap-4 justify-center items-stretch sm:items-center";

export const gradientCtaPrimaryClass =
  "inline-flex items-center justify-center min-h-[3.75rem] px-8 sm:px-10 py-3.5 sm:py-4 rounded-full font-['Poppins',sans-serif] font-semibold text-base sm:text-lg transition-colors bg-white text-[#1C3957] hover:bg-gray-50";

export const gradientCtaOutlineClass =
  "inline-flex items-center justify-center min-h-[3.75rem] px-8 sm:px-10 py-3.5 sm:py-4 rounded-full font-['Poppins',sans-serif] font-semibold text-base sm:text-lg transition-colors border border-white text-white hover:bg-white/10";

/** Shadcn Button `className` on gradient CTAs — use with `size="lg"` and `h-auto`. */
export const gradientCtaPrimaryButtonClass =
  "min-h-[3.75rem] h-auto px-8 sm:px-10 py-3.5 sm:py-4 rounded-full text-base sm:text-lg bg-white text-[#1C3957] hover:bg-gray-50 font-semibold font-['Poppins',sans-serif]";

export const gradientCtaOutlineButtonClass =
  "min-h-[3.75rem] h-auto px-8 sm:px-10 py-3.5 sm:py-4 rounded-full text-base sm:text-lg border-white text-white hover:bg-white/10 font-semibold font-['Poppins',sans-serif]";
