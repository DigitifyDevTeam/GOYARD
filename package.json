{
  "name": "brasenplus",
  "private": true,
  "description": "Root workspace – frontend (react) + backend (Python)",
  "scripts": {
    "sonar": "python scripts/sonar_analyze_and_report.py",
    "sonar:fetch": "python scripts/sonar_analyze_and_report.py --fetch-only"
  }
}
